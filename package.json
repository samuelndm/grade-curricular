{
  "name": "grade-curricular",
  "version": "1.0.0",
  "description": "http://grade-curricular.herokuapp.com/",
  "scripts": {
    "ng": "ng",
    "start": "npm run start-server",
    "start-server": "cd ./server && npm install && node server.js",
    "heroku-prebuild": "cd ./client && npm install",
    "heroku-postbuild": "cd ./client && ng build --prod"
    
  },
  "dependencies": {
    "@angular/cli": "~8.3.23"
  },
  "devDependencies": {
    "standard": "^14.3.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/samuelndm/grade-curricular.git"
  },
  "license": "ISC",
  "engines": {
    "node": "12.14.1",
    "npm": "6.13.4"
  }
}
