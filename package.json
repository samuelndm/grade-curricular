{
  "name": "grade-curricular",
  "version": "1.0.0",
  "description": "http://grade-curricular.herokuapp.com/",
  "scripts": {
    "ng": "ng",
    "start": "npm run start-server",
    "start-server": "npm install --prefix ./server && cd ./server && node server.js",
    "heroku-prebuild": "npm install --prefix ./client",
    "heroku-postbuild": "cd ./client && npm start"
    
  },
  "dependencies": {
  },
  "devDependencies": {
    "standard": "^14.3.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/samuelndm/grade-curricular.git"
  },
  "license": "ISC",
  "engines": {
    "node": "12.14.1",
    "npm": "6.13.4"
  }
}
